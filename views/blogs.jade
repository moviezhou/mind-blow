extends layout

block content
  #wrapper
    #logo
      img(src='/images/logo.png')
    #display
      include userbar
    article.content
      ul.listing
        -for (var id in blogs)
          -var blog = blogs[id]
          li.listing-item
            a(href='/blog/#{id}') #{blog.title}

    include footer